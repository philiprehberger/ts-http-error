# Changelog

## 0.1.1

- Standardize package metadata, badges, and CHANGELOG

## 0.1.0

- `HttpError` class with status codes and default messages
- Factory methods for common HTTP errors (400-503)
- `isHttpError()` type guard with optional status filtering
- `toJSON()` serialization
